---
description: Save current changes to GitHub
---

1. Add all changes
// turbo
```bash
git add .
```

2. Commit changes (prompt user for message if not provided, otherwise use a default "Update")
// turbo
```bash
git commit -m "Update project files"
```

3. Push to main branch
// turbo
```bash
git push origin main
```
