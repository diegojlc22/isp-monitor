# ISP Monitor - Configuration
# Copy this file to .env and adjust values for your environment

# ===== Security Configuration =====
# Admin user credentials (já configurado com suas credenciais)
# Só precisa mudar se quiser usar outras credenciais
ADMIN_EMAIL=diegojlc22@gmail.com
ADMIN_PASSWORD=110812

# CORS allowed origins (comma-separated)
# Development: http://localhost:5173,http://localhost:3000
# Production: https://monitor.yourcompany.com
CORS_ORIGINS=http://localhost:5173,http://localhost:3000

# ===== Database Configuration =====
# For production with 800+ devices, use PostgreSQL (optional)
# DATABASE_URL=postgresql+asyncpg://user:password@localhost:5432/isp_monitor

# ===== Ping Configuration =====
# Interval between ping cycles (seconds)
# Default: 30s for production (5s for development)
PING_INTERVAL_SECONDS=30

# Ping timeout (seconds)
PING_TIMEOUT_SECONDS=2

# Maximum concurrent pings
# Adjust based on your server capacity
PING_CONCURRENT_LIMIT=100

# ===== Log Retention =====
# Keep ping logs for N days (older logs are deleted automatically)
LOG_RETENTION_DAYS=30

# ===== Redis Cache (Optional - Recommended for 500+ devices) =====
# Enable Redis caching for dashboard data
REDIS_ENABLED=false

# Redis connection URL
REDIS_URL=redis://localhost:6379/0

# Cache TTL in seconds (how long to cache dashboard data)
CACHE_TTL_SECONDS=60
